# v2.10 plan index

**Status:** Scope remains in [`todo.md`](../../../todo.md). Settings/navigation/shortcuts, click-away rename, the rail logo, and Whisper choice policy Slice A have shipped. Hardware-gated slices remain unimplemented. Linked designs are not acceptance evidence.

Use this page for **order and remaining investigations**. Use each feature plan for behavior, files, and tests. Use `todo.md` to check items off.

## What is already done

- v2.9.0 is released.
- File-level designs exist for inference, Parakeet, Whisper language/model policy, summarisation languages/models, and Settings / navigation / shortcuts.
- Inference-performance Task 5 recorded a 2026-09-14 Apple Silicon decision: retain Opus effort 10, existing MLX Whisper defaults, and 32k summary context. No production default changed. See [inference evidence](../../development/V2_10_INFERENCE_PERFORMANCE.md).
- Settings and navigation presentation, keyboard shortcuts, and click-away meeting rename shipped in PR #103. The AvaNevis rail logo shipped in PR #104.
- Whisper choice policy Slice A shipped in PR #102: Persian is off new choices; Tiny/Base are retired; Small is the floor; pending jobs keep their saved language/model.
- Linux Parakeet CPU and CUDA 12 ONNX feasibility ran on CachyOS on 2026-09-21;
  representative meeting benefit remains unqualified. A 2026-09-22 integrity
  and live managed CUDA probe recheck passed. See [evidence](../../development/PARAKEET_COMPATIBILITY.md).
- Linux microphone volume investigation has not started.

## Active next task — CachyOS only

Execute [CachyOS Parakeet qualification](2026-09-22-cachyos-parakeet-qualification.md):
keep the working CUDA 12 candidate fixed, build a reproducible developer harness,
and screen one representative English meeting against Whisper Small. Establish
benefit before expanding acceptance or implementing the engine. CPU repeats,
Windows, macOS, and Omarchy acceptance are deferred for this task; the broader
release targets below are unchanged. No other v2.10 feature waits on this result.

## Recommended order

Work in three lanes. They can overlap; they are not a single global queue.

### Lane 1 — start without hardware qualification

These do not expose a new model or claim a new language/runtime. The committed Lane 1 items have shipped:

1. **Settings and navigation presentation** ([plan](2026-09-07-settings-navigation-shortcuts.md) slice A): shipped in PR #103. Linux Speakrs as static text, enable/disable wording, AI Add-on log timestamps, Record rail icon, Speakrs removal that leaves Hugging Face tokens untouched. Rail logo followed in PR #104.
2. **Keyboard shortcuts** (same plan, slice B): shipped in PR #103. App-focused chords; packaged conflict checks on Windows, macOS, and supported Linux remain a release-host check.
3. **Click-away meeting rename:** shipped in PR #103 with the Settings/shortcuts work. No separate design document.
4. **Whisper choice policy** ([plan](2026-09-07-language-model-policy-whisper-large.md) slice A): shipped in PR #102. Persian, Tiny, and Base are off **new** selections; saved Tiny/Base migrate to Small; pending jobs keep their saved language/model.

Remaining Slice A design extras (label copy, Start language gate, explicit Download/Cancel/Retry versus auto-FTUE) are not required to close the two curated-choice scope bullets.

### Lane 2 — investigations (eight areas still open)

Run these before the matching feature ships. CPU evidence does not qualify CUDA, and the reverse is also true. Linux summaries and some add-ons stay gated on managed CUDA; skip those Linux rows when the gate fails, and do not invent a CPU fallback.

| # | Investigation | Where to run it | Blocks |
| --- | --- | --- | --- |
| 1 | Parakeet vs Whisper Small: Linux feasibility complete; representative meeting screening next | **Active: CachyOS x86_64 managed CUDA 12.** CPU repeats and other hosts deferred; each future platform/device still needs independent qualification | Optional Parakeet engine on that platform |
| 2 | Whisper Large v3 identity, cache/download, RAM/VRAM, ordinary and guided timings | Windows x64 CPU/CUDA 12; Apple Silicon macOS; Linux x86_64 CPU/managed CUDA, independently | Exposing Large |
| 3 | Other listed transcription languages (not Persian) | Representative local fixtures plus fluent review on the relevant Whisper runtime. Test Mandarin and Cantonese separately; one `zh` code is not both. | Extra language removals |
| 4 | Current Qwen summary languages | Windows x64 CUDA; Apple Silicon Metal; Linux x86_64 **managed CUDA 12 only** | Summary language gating and “supported language” claims |
| 5 | Local check that summary prose matches the requested language | Prove a method on one runtime first; repeat on each pinned llama.cpp if promoted | Enforced output-language matching |
| 6 | Alternative summary models vs installed Qwen 9B | Same GPU rows as (4). Start with catalog 4B/14B entries, then a short newer-GGUF list. Keeping Qwen is valid. | Replacing Qwen or adding a model selector |
| 7 | PipeWire/PulseAudio microphone reset to 50% | Supported Omarchy/CachyOS Linux desktop session only | Any app-side or setup-side volume change |
| 8 | Inference performance on Windows and Linux | Only if a speed default is still being pursued. Apple Silicon already rejected the tested encoding, Whisper, and smaller-context candidates. | Changing those production defaults |

Create public notes during those investigations (`PARAKEET_COMPATIBILITY.md`, `V2_10_LANGUAGE_MODEL_QUALIFICATION.md`, `V2_10_SUMMARY_QUALIFICATION.md`). Do not commit user recordings, transcripts, prompts, tokens, scratch report directories, or home-directory paths. Identify hardware class, OS, app revision, and artifact hashes.

### Lane 3 — implement after the matching investigation

- **Parakeet:** after (1) pins a runtime per platform. Unqualified combinations stay unavailable. [Plan](2026-09-06-parakeet-integration.md).
- **Whisper Large:** after (2). Independent per platform/runtime is acceptable. [Plan](2026-09-07-language-model-policy-whisper-large.md) slice B.
- **Further language removals:** after (3) and a product review. Persian already left new choices in Slice A.
- **Summary language UI and enforcement:** after (4), and (5) if output language will be enforced. [Plan](2026-09-07-summary-languages-and-models.md) slice A.
- **Summary model selector or replacement:** after (6), only if a candidate qualifies. Slice B.
- **Inference default changes:** none are qualified today. Reopen only with (8) plus the quality gates in the [inference plan](2026-09-06-inference-performance.md). Persistent workers need their own design.
- **Linux volume behavior:** only after (7), and only if a remedy is safe.

Packaged acceptance still happens on Windows 10/11 x64, Apple Silicon macOS 14+, and supported Omarchy/CachyOS Linux. CI is not hardware acceptance.

## Feature designs

- [Inference performance](2026-09-06-inference-performance.md) — baselines and independent encode / Whisper / summary-context slices. Apple Silicon decision is retain-current. Workers are out of this plan.
- [Parakeet integration](2026-09-06-parakeet-integration.md) — optional English-only engine. Qualify, then implement. Keep Whisper as default.
- [Language/model policy and Whisper Large](2026-09-07-language-model-policy-whisper-large.md) — selectable policy vs pending-job compatibility; independently gated Large v3; retained-language evaluation.
- [Qwen language support and alternative summarisation models](2026-09-07-summary-languages-and-models.md) — current-model language qualification; optional alternative-model evaluation; selector only if more than one model qualifies.
- [Settings, navigation and keyboard shortcuts](2026-09-07-settings-navigation-shortcuts.md) — presentation and app-focused shortcuts. Click-away rename stays separate.

Linux microphone volume still needs an investigation note; this index does not invent it. Click-away rename shipped in PR #103 with the Settings/shortcuts work.
