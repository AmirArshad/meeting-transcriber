# Common dependencies shared by both Windows and macOS

# Audio processing
numpy>=2.4.6            # For audio processing and mixing
soxr>=0.3.0              # For high-quality audio resampling

# Transcription
filelock>=3.20.3         # Prevent race conditions during model downloads (CVE-2025-68146, CVE-2026-22701)
