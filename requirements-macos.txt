# Audio Recording Dependencies (macOS)
sounddevice>=0.4.6       # Cross-platform audio I/O (for microphone)
numpy>=1.24.0            # For audio processing and mixing
soxr>=0.3.0              # For high-quality audio resampling (VHQ quality)
pyobjc-framework-ScreenCaptureKit>=10.0  # macOS ScreenCaptureKit for desktop audio (fallback)
pyobjc-framework-CoreAudio>=10.0         # macOS CoreAudio support
pyobjc-framework-AVFoundation>=10.0      # macOS AVFoundation for audio

# Transcription Dependencies (macOS with Metal GPU)
lightning-whisper-mlx>=0.0.10  # MLX-based Whisper for Apple Silicon Metal GPU
scipy>=1.11.0                  # Required by lightning-whisper-mlx for timing/alignment
filelock>=3.12.0               # Prevent race conditions during model downloads

# Note: lightning-whisper-mlx uses Apple's MLX framework for Metal GPU acceleration
# This provides 2-3x realtime performance on Apple Silicon (M1/M2/M3/M4)
# macOS builds target Apple Silicon only - Intel Macs are not supported
