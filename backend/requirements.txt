# Audio Recording Dependencies
pyaudiowpatch>=0.2.12.8  # PyAudio fork with WASAPI loopback support for Windows
numpy>=2.5.1            # For audio processing and mixing

# Future dependencies (commented out for now)
# faster-whisper>=1.0.0    # For transcription
# torch>=2.0.0             # For GPU support
