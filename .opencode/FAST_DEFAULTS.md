# Fast Agent Defaults

These live in root `AGENTS.md` under **Working defaults**, which OpenCode already loads on every request. Read that section — it is authoritative and this file intentionally does not restate it.

Short version: work inline, ask before spawning a subagent, smallest relevant test first.
