---
description: Recorder stdout JSON contract and Electron parser coupling
globs: backend/audio/**,src/main.js,src/main/recorder-service.js,src/main-process-helpers.js,src/main-process/recorder-output-helpers.js,tests/js/main-process-helpers.test.js,tests/js/recorder-event-contract.test.js
alwaysApply: false
---

# Recorder stdout contract

You are editing the recorder control contract. In `AGENTS.md`, read **Critical invariants → Recorder stdout is the control contract**, then **Recording data-loss guards** and **Post-processing mix architecture**.

Highest-cost mistakes here: driving control flow from stderr, changing startup/progress on one side only, and exiting a failed stop without a structured `success:false` payload.
