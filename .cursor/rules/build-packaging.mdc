---
description: Build packaging — prepared resources, manifests, updater names
globs: build/**,package.json,src/updater.js,.github/workflows/build-release.yml,.github/workflows/ci.yml
alwaysApply: false
---

# Build and release packaging

You are editing packaging. In `AGENTS.md`, read **Critical invariants → Build packaging** — it carries the four files that must stay aligned, the `AVANEVIS_PACKAGED=1` path-hardening rule, and the updater filename coupling.

Step-by-step procedures: `docs/development/BUILD_INSTRUCTIONS.md`, `docs/development/INSTALLER_IMPLEMENTATION.md`.
