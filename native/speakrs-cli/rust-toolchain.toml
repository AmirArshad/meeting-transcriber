[toolchain]
channel = "1.88.0"
components = ["rustfmt", "clippy"]
# Packaging builds pass an explicit --target (aarch64-apple-darwin or
# x86_64-pc-windows-msvc). CI/release and prepare-resources install that
# rust-std rather than listing both targets here.
