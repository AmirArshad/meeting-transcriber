# Audio Recording Dependencies (Windows)
pyaudiowpatch>=0.2.12.4  # PyAudio fork with WASAPI loopback support for Windows
numpy>=1.24.0            # For audio processing and mixing
scipy>=1.11.0            # For audio enhancement (filtering, resampling)
soxr>=0.3.0              # For high-quality audio resampling

# Transcription Dependencies
faster-whisper>=1.0.0    # Fast, accurate Whisper implementation (CPU + GPU)
filelock>=3.29.0         # Prevent race conditions during model downloads

# Optional local speaker identification add-on
# The desktop app installs pyannote.audio into userData during explicit speaker setup.
# For backend-only development, install pyannote.audio separately in a Python 3.11 env.
# Set AVANEVIS_SKIP_MANAGED_DIARIZATION_DEPS=1 to use your active env instead.

# GPU Acceleration (Optional but recommended for faster transcription)
# For NVIDIA GPU users with CUDA installed:
# Install CuBLAS and cuDNN for faster-whisper/CTranslate2:
#    pip install nvidia-cublas-cu12 nvidia-cudnn-cu12
#
# Note: Speaker diarization uses a separate managed PyTorch CUDA setup only when
# the user explicitly enables that add-on.
