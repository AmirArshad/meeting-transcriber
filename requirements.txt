# Audio Recording Dependencies
pyaudiowpatch>=0.2.12.8  # PyAudio fork with WASAPI loopback support for Windows
numpy>=2.4.0            # For audio processing and mixing
soxr>=1.1.0              # For high-quality audio resampling (Windows recorder path)

# Transcription Dependencies
faster-whisper>=1.0.0    # Fast, accurate Whisper implementation (CPU + GPU)
filelock>=3.31.0         # Prevent race conditions during model downloads

# GPU Acceleration (Optional but recommended for faster transcription)
# For NVIDIA GPU users with CUDA installed:
# Install CuBLAS and cuDNN for faster-whisper/CTranslate2:
#    pip install nvidia-cublas-cu12 nvidia-cudnn-cu12
#
# Speaker diarization uses separate managed PyTorch CUDA dependencies only when
# the user explicitly enables that add-on.
